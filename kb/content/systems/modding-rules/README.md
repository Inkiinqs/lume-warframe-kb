# Modding Rules

This folder stores buildcraft rules that apply across many loadouts.

Examples:

- elemental combination order
- additive versus multiplicative stacking
- faction damage placement
- special mod restrictions
- exalted and pseudo-exalted edge cases

These records give the AI the reasoning layer it needs for build generation.
