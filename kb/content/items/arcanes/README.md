# Arcanes

This folder stores one canonical record per Arcane.

Suggested contents:

- trigger conditions
- bonuses and stack behavior
- loadout slot compatibility
- acquisition references

Shared proc and scaling behavior should still point back to `content/systems/`.
