# Mappings

This folder stores source-to-canonical mapping rules.

The first mapping file is:

- `static-import.map.json`

Use mapping files to keep importer logic generic while source-specific field wiring stays declarative.
