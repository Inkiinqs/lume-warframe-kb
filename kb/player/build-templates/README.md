# Build Templates

Use this folder for saved build snapshots and AI-generated recommendations.

Recommended boundaries:

- one file for the build shell and metadata
- separate slot/mod details in the `data` object
- reference canonical item IDs instead of repeating item descriptions
