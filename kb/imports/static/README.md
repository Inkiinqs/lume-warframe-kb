# Static Imports

Use this folder for raw snapshots of mostly stable game data.

Suggested subfolders:

- `source-snapshots/` for untouched upstream payloads
- `normalized/` for cleaned intermediate records before canonical write
- `manifests/` for source run metadata

Static imports should feed:

- items
- world
- activities
- non-rotating relationships
