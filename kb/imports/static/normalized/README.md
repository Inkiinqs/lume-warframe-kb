# Normalized Preview Outputs

This folder stores generated previews or intermediate outputs from static normalization runs.

Current convention:

- `last-static-normalization.json` is the latest preview payload produced by `normalize-static-import.ps1`

Treat files here as build artifacts, not canonical game records.
