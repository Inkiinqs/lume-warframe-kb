# AI Layer

The AI layer is derived, not primary.

Use this folder for:

- retrieval-friendly search documents
- embeddings
- materialized views for build and farming recommendations
- query-router contracts that map assistant intents to views and content scopes
- prompt references and system hints

Do not hand-author gameplay truth here when it already exists in `content/`.
